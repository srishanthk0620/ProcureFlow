import { AppShell } from '../../components/layout/AppShell'
import { Button, Card, PageHeader } from '../../components/ui/Primitives'
import { Building2, CircleAlert, Timer, FlaskConical } from 'lucide-react'
import { Summary } from '../../components/ui/DashboardParts'
export function AdminDashboard() {
  return <AppShell role="Administrator"><PageHeader eyebrow="Administrator workspace" title="Procurement Network" description="The wider view of your centres and the communities they serve." /><Summary labels={['Centres online', 'Farmers scheduled', 'Network load', 'Average wait']} /><div className="admin-grid">{[{ title: 'Centre overview', text: 'Connected centres and their operational status will appear here.', icon: Building2, id: 'centres' }, { title: 'Bottleneck monitoring', text: 'Stage-level constraints will be visible when centre resources are connected.', icon: CircleAlert, id: 'monitoring' }, { title: 'Scheduling performance', text: 'Scheduling insights will appear once appointment data is available.', icon: Timer, id: 'performance' }].map(({ title, text, icon: Icon, id }) => <Card key={id}><h2 id={id}>{title}</h2><div className="empty-state"><Icon size={32} strokeWidth={1.5} aria-hidden="true" /><strong>Not connected yet</strong><p>{text}</p></div></Card>)}<Card><h2>Disruption simulation</h2><div className="empty-state"><FlaskConical size={32} strokeWidth={1.5} aria-hidden="true" /><Button className="secondary" disabled aria-describedby="disruption-help">Run demo disruption</Button><p id="disruption-help">Available after the scheduling engine is connected.</p></div></Card></div></AppShell>
}
